
Just like Apache Bench

> Boom! is a simple command line tool to send some load to a web app..

## Usage

```sh
$ docker run --rm melvinlee/boom http://localhost:80 -c 10 -n 100
```

```sh
$ docker run --rm melvinlee/boom --help
```

